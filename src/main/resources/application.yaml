server:
  address: localhost
  port: 9090
  servlet.context-path: /console

spring:
  main:
    banner-mode: console
  application:
    name: aquariuxtrd
    version: 1.0.0-SNAPSHOT
  datasource:
    username: admin
    password: password
    url: 'jdbc:h2:file:./.local-volumes/data/aquariuxtrd;DB_CLOSE_ON_EXIT=FALSE'
    driverClassName: 'org.h2.Driver'
  h2:
    console:
      enabled: true
      path: '/h2-ui'
      settings.web-allow-others: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate.ddl-auto: update
    show-sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui

app:
  client:
    binance.url: https://api.binance.com/api/v3/ticker/bookTicker
    houbi.url: https://api.huobi.pro/market/tickers
  supported-cryptos: >
    BTCUSDT,ETHUSDT
  price-collector:
    fixed-rate-ms: 10_000
